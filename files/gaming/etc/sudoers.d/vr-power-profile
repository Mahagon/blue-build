# Allow wheel users to switch the AMD GPU power profile without a password.
# Scoped to the specific sysfs path used by the vr-on/vr-off shell functions.
%wheel ALL=(root) NOPASSWD: /usr/bin/tee /sys/class/drm/card[0-9]*/device/pp_power_profile_mode
